<Page x:Class="De.Hsfl.LoomChat.Client.Views.MainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:De.Hsfl.LoomChat.Client.ViewModels"
      xmlns:converter="clr-namespace:De.Hsfl.LoomChat.Client.Converter"
      Title="MainView"
      Loaded="MainViewLoaded">

    <!-- 
        1. DataContext ist dein MainViewModel
        2. In den Resources legen wir BoolToVisibilityConverter und UserIdToNameConverter an
        3. Wir verwenden im "OtherMessageTemplate" unten ein MultiBinding
           (SenderUserId und Users). 
    -->

    <Page.DataContext>
        <local:MainViewModel />
    </Page.DataContext>

    <Page.Resources>
        <!-- Um Boolean -> Visibility zu konvertieren -->
        <converter:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />

        <!-- Unser IMultiValueConverter, der (SenderUserId, Users) -> Username liefert -->
        <converter:UserIdToNameConverter x:Key="UserIdToNameConverter" />

        <!-- Template: eigene Nachrichten (rechts, grün) -->
        <DataTemplate x:Key="MyMessageTemplate">
            <Border Background="#DCF8C6"
                    Padding="10"
                    Margin="10"
                    CornerRadius="10"
                    HorizontalAlignment="Right">
                <StackPanel>
                    <!-- Beispielhaft "Ich" -->
                    <TextBlock Text="Ich"
                               FontWeight="Bold"
                               Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding Content}" />
                    <TextBlock Text="{Binding SentAt, StringFormat=HH:mm:ss}"
                               FontSize="10"
                               HorizontalAlignment="Right"/>
                </StackPanel>
            </Border>
        </DataTemplate>

        <!-- Template: fremde Nachrichten (links, grau) -->
        <DataTemplate x:Key="OtherMessageTemplate">
            <Border Background="LightGray"
                    Padding="10"
                    Margin="10"
                    CornerRadius="10"
                    HorizontalAlignment="Left">
                <StackPanel>
                    <!-- MultiBinding: [0] => SenderUserId, [1] => Users -->
                    <TextBlock>
                        <TextBlock.Text>
                            <MultiBinding Converter="{StaticResource UserIdToNameConverter}">
                                <!-- Der SenderUserId kommt direkt vom ChatMessageDto -->
                                <Binding Path="SenderUserId"/>
                                <!-- Die Liste Users holen wir per "DataContext.Users" vom übergeordneten Page -->
                                <Binding Path="DataContext.Users" 
                                         RelativeSource="{RelativeSource AncestorType=Page}" />
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>

                    <TextBlock Text="{Binding Content}" />
                    <TextBlock Text="{Binding SentAt, StringFormat=HH:mm:ss}"
                               FontSize="10"
                               HorizontalAlignment="Right"/>
                </StackPanel>
            </Border>
        </DataTemplate>

        <!-- TemplateSelector: je nach SenderUserId => MyMessageTemplate oder OtherMessageTemplate -->
        <converter:MessageTemplateSelector x:Key="MessageTemplateSelector"
                                           MyMessageTemplate="{StaticResource MyMessageTemplate}"
                                           OtherMessageTemplate="{StaticResource OtherMessageTemplate}" />
    </Page.Resources>


    <!-- Hauptlayout: zwei Zeilen, zwei Spalten -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="15*"/>
            <RowDefinition Height="85*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20*"/>
            <ColumnDefinition Width="80*"/>
        </Grid.ColumnDefinitions>

        <!-- Beispielhafte Kopfzeile (Row=0, ColSpan=2) -->
        <Border Grid.Row="0"
                Grid.ColumnSpan="2"
                Padding="10"
                Background="#E8E8E8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="LoomChat"
                           FontSize="24"
                           FontWeight="Bold"
                           VerticalAlignment="Center"/>

                <StackPanel Orientation="Horizontal" Grid.Column="1" VerticalAlignment="Center">
                    <Button Content="Logout"
                            Command="{Binding LogoutCommand}"
                            Width="100"
                            Height="30"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Linke Spalte (Row=1, Col=0) -->
        <Border Grid.Row="1"
                Grid.Column="0"
                Background="#F4F4F4"
                Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <!-- Direktnachrichten, Button, Channels, Users -->
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Direktnachrichten (Row=0) -->
                <GroupBox Grid.Row="0" Header="Direktnachrichten" Margin="0,0,0,10">
                    <ListView ItemsSource="{Binding DirectMessages}"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              MouseDoubleClick="ChannelListViewMouseDoubleClick">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" FontSize="14"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </GroupBox>

                <!-- Button "Channel erstellen" (Row=1) -->
                <Button Grid.Row="1"
                        Content="Channel erstellen"
                        Command="{Binding OpenPopupCommand}"
                        Width="120"
                        Height="30"
                        Margin="0,0,0,10"/>

                <!-- Channels (Row=2) -->
                <GroupBox Grid.Row="2" Header="Channels" Margin="0,0,0,10">
                    <ListView ItemsSource="{Binding OpenChats}"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              MouseDoubleClick="ChannelListViewMouseDoubleClick">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" FontSize="14"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </GroupBox>

                <!-- Users (Row=3) -->
                <GroupBox Grid.Row="3" Header="User">
                    <ListView ItemsSource="{Binding Users}"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              MouseDoubleClick="UsersListViewMouseDoubleClick">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Username}" FontSize="14"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </GroupBox>
            </Grid>
        </Border>

        <!-- Rechte Spalte (Row=1, Col=1) für den Chat -->
        <Border Grid.Row="1"
                Grid.Column="1"
                Padding="10"
                Background="#F8F8F8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Wenn ein Channel ausgewählt, zeige den Chat, sonst "Bitte wähle" -->
                <Grid Visibility="{Binding ChatVisible, Converter={StaticResource BoolToVisibilityConverter}}"
                      Grid.RowSpan="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Nachrichtenanzeige (oben) -->
                    <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding SelectedChannel.ChatMessages}"
                                      ItemTemplateSelector="{StaticResource MessageTemplateSelector}">
                            <!-- Hier kein ItemTemplate, da wir TemplateSelector benutzen -->
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- Neue Nachricht eingeben (unten) -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBox Text="{Binding NewMessage}"
                                 VerticalAlignment="Center"
                                 Width="500"
                                 Margin="0,0,10,0"/>
                        <Button Content="Senden"
                                Command="{Binding SendMessageCommand}"
                                Width="100"
                                Height="30"/>
                    </StackPanel>
                </Grid>

                <!-- Platzhalter: kein Channel ausgewählt -->
                <TextBlock Grid.Row="0"
                           Text="Bitte wähle einen Chat aus."
                           FontSize="16"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="{Binding IsChatNotVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </Grid>
        </Border>

        <!-- Popup zum Erstellen eines Channels -->
        <Popup x:Name="CreateChannelPopup"
               Placement="Center"
               IsOpen="{Binding PopupOpen}"
               StaysOpen="False">
            <Border Background="White"
                    BorderBrush="Gray"
                    BorderThickness="1"
                    Padding="20"
                    Width="300">
                <StackPanel>
                    <TextBlock Text="Neuen Channel erstellen"
                               FontSize="16"
                               FontWeight="Bold"
                               Margin="0,0,0,10"/>
                    <TextBox Text="{Binding NewChannelName}" Width="300" Margin="0,0,0,10"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Abbrechen"
                                Command="{Binding ClosePopupCommand}"
                                Width="80"
                                Margin="5"/>
                        <Button Content="Erstellen"
                                Command="{Binding CreateChannelCommand}"
                                Width="80"
                                Margin="5"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Popup>

    </Grid>
</Page>
