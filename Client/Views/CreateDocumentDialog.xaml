<Window x:Class="De.Hsfl.LoomChat.Client.Views.CreateDocumentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Neues Dokument"
        WindowStartupLocation="CenterOwner"
        Height="150"
        Width="350">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Dokument-Name:" Grid.Row="0" Margin="0,0,0,5"/>
        <TextBox x:Name="DocumentNameTextBox"
                 Grid.Row="1"
                 Margin="0,0,0,5"/>

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Grid.Row="2">
            <Button Content="OK" Width="80" Margin="5" Click="OkButtonClick"/>
            <Button Content="Abbrechen" Width="80" Margin="5" Click="CancelButtonClick"/>
        </StackPanel>
    </Grid>
</Window>
